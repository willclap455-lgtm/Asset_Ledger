@extends('layouts.app')

@section('content')
<div class="d-flex justify-content-between align-items-center mb-3"><div><div class="page-title">Inventory Movements</div><div class="text-muted">Receiving, deployment, transfer, return, repair, swap, and retirement history.</div></div><a class="btn btn-primary btn-sm" href="{{ route('movements.create') }}">Record Movement</a></div>
<form method="GET" class="card card-body py-2 mb-3"><div class="row g-2 align-items-end"><div class="col-md-3"><label class="form-label mb-1">Type</label><select class="form-select form-select-sm" name="type"><option value="">All</option>@foreach($types as $key=>$label)<option value="{{ $key }}" @selected(request('type')===$key)>{{ $label }}</option>@endforeach</select></div><div class="col-md-4"><label class="form-label mb-1">Client</label><select class="form-select form-select-sm" name="client_id"><option value="">All</option>@foreach($clients as $client)<option value="{{ $client->id }}" @selected(request('client_id')===$client->id)>{{ $client->name }}</option>@endforeach</select></div><div class="col-md-2"><button class="btn btn-secondary btn-sm">Filter</button></div></div></form>
<div class="card"><div class="table-responsive"><table class="table table-sm table-hover mb-0"><thead><tr><th>Movement #</th><th>Type</th><th>Items</th><th>Client</th><th>From</th><th>To</th><th>User</th><th>Date</th><th></th></tr></thead><tbody>@forelse($movements as $movement)<tr><td><a class="text-mono" href="{{ route('movements.show', $movement) }}">{{ $movement->movement_number }}</a></td><td>{{ $types[$movement->movement_type] ?? $movement->movement_type }}</td><td>{{ $movement->lines_count }}</td><td>{{ $movement->client?->name ?? 'Internal' }}</td><td>{{ $movement->fromLocation?->label() ?? 'N/A' }}</td><td>{{ $movement->toLocation?->label() ?? 'N/A' }}</td><td>{{ $movement->user?->name }}</td><td>{{ $movement->occurred_at->format('m/d/Y H:i') }}</td><td class="text-end">@can('update', $movement)<a class="btn btn-outline-primary btn-sm" href="{{ route('movements.edit', $movement) }}">Edit</a>@endcan</td></tr>@empty<tr><td colspan="9" class="text-muted">No movement history yet.</td></tr>@endforelse</tbody></table></div><div class="card-footer py-2">{{ $movements->links() }}</div></div>
@endsection
