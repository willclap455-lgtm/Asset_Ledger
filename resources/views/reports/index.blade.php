@extends('layouts.app')
@section('content')
<div class="d-flex justify-content-between align-items-center mb-3"><div><div class="page-title">Reports</div><div class="text-muted">Operational reporting for inventory posture, carriers, repairs, and traceability.</div></div><a class="btn btn-outline-primary btn-sm" href="{{ route('reports.inventory-export') }}">Export Inventory CSV</a></div>
<div class="row g-3"><div class="col-lg-4"><div class="card"><div class="card-header fw-semibold">Inventory by Client</div><div class="card-body">@foreach($byClient as $row)<div class="d-flex justify-content-between border-bottom py-1"><span>{{ $row->label }}</span><strong>{{ $row->total }}</strong></div>@endforeach</div></div></div><div class="col-lg-4"><div class="card"><div class="card-header fw-semibold">Inventory by Location</div><div class="card-body">@foreach($byLocation as $row)<div class="d-flex justify-content-between border-bottom py-1"><span>{{ $row->label }}</span><strong>{{ $row->total }}</strong></div>@endforeach</div></div></div><div class="col-lg-4"><div class="card"><div class="card-header fw-semibold">Devices by Carrier</div><div class="card-body">@foreach($byCarrier as $row)<div class="d-flex justify-content-between border-bottom py-1"><span>{{ $row->carrier ?: 'Unknown' }}</span><strong>{{ $row->total }}</strong></div>@endforeach</div></div><div class="card mt-3"><div class="card-body"><div class="text-muted small">Movements in last 30 days</div><div class="fs-2 fw-bold">{{ $recentMovementCount }}</div></div></div></div><div class="col-lg-6"><div class="card"><div class="card-header fw-semibold">Phones with Outdated Android Versions</div><div class="table-responsive"><table class="table table-sm mb-0"><thead><tr><th>Asset</th><th>Version</th><th>Model</th></tr></thead><tbody>@foreach($outdatedAndroid as $item)<tr><td><a href="{{ route('inventory-items.show',$item) }}">{{ $item->asset_tag }}</a></td><td>{{ $item->phone?->android_version }}</td><td>{{ $item->manufacturer }} {{ $item->model }}</td></tr>@endforeach</tbody></table></div></div></div><div class="col-lg-6"><div class="card"><div class="card-header fw-semibold">Printer Repair Frequency</div><div class="table-responsive"><table class="table table-sm mb-0"><thead><tr><th>Printer</th><th>Repairs</th></tr></thead><tbody>@foreach($printerRepairFrequency as $row)<tr><td class="text-mono">{{ $row->asset_tag }}</td><td>{{ $row->repair_count }}</td></tr>@endforeach</tbody></table></div></div></div></div>
@endsection
