@extends('layouts.app')
@section('content')
<div class="d-flex justify-content-between align-items-center mb-3"><div><div class="page-title">{{ $client->name }}</div><div class="text-muted">{{ $client->code }} - {{ $client->status }}</div></div><div class="btn-group"><a class="btn btn-primary btn-sm" href="{{ route('clients.edit', $client) }}">Edit</a>@can('delete', $client)<form method="POST" action="{{ route('clients.destroy', $client) }}" onsubmit="return confirm('Delete this client? Related locations and inventory will become unassigned.')">@csrf @method('DELETE')<button class="btn btn-outline-danger btn-sm">Delete</button></form>@endcan</div></div>
<div class="row g-3"><div class="col-lg-4"><div class="card"><div class="card-header fw-semibold">Contact</div><div class="card-body"><div>{{ $client->primary_contact_name }}</div><div>{{ $client->primary_contact_email }}</div><div>{{ $client->primary_contact_phone }}</div><hr><div class="small text-muted">{{ $client->notes }}</div></div></div></div><div class="col-lg-8"><div class="card mb-3"><div class="card-header fw-semibold">Locations</div><div class="list-group list-group-flush">@foreach($client->locations as $location)<div class="list-group-item d-flex justify-content-between"><span>{{ $location->name }}</span><span class="text-muted small">{{ $location->city }} {{ $location->state }}</span></div>@endforeach</div></div><div class="card"><div class="card-header fw-semibold">Assigned Inventory</div><div class="table-responsive"><table class="table table-sm mb-0"><thead><tr><th>Asset</th><th>Type</th><th>Status</th><th>Location</th></tr></thead><tbody>@foreach($client->inventoryItems as $item)<tr><td><a href="{{ route('inventory-items.show', $item) }}">{{ $item->asset_tag }}</a></td><td>{{ $item->item_type }}</td><td>{{ $item->status }}</td><td>{{ $item->location?->name }}</td></tr>@endforeach</tbody></table></div></div></div></div>
@endsection
